import setuptools

setuptools.setup(
    name="bush-dr-measurement",
    description="Package that uses the bush et al methodology to measure bgp default routing in the internet",
    packages=setuptools.find_packages(),
    python_requires=">=3.6"
)
